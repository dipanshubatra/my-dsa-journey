class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
            from collections import defaultdict
            angram = defaultdict(list)
            for word in strs:
                key = tuple(sorted(word))
                angram[key].append(word)
            return list(angram.values())    
